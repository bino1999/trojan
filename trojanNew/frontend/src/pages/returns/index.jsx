export default function Returns() {
  return (
    <div>
      <h1 className="text-2xl font-semibold mb-1">Returns</h1>
      <p className="text-muted-foreground text-sm mb-6">Process customer returns from sales or service jobs</p>
      {/* TODO: returns list */}
      {/* TODO: new return — look up sale/job by number, select items + partial qty, enter reason */}
      {/* TODO: save → stock auto-incremented, refund record created */}
    </div>
  )
}
