export default function InternalUse() {
  return (
    <div>
      <h1 className="text-2xl font-semibold mb-1">Internal Use</h1>
      <p className="text-muted-foreground text-sm mb-6">Log items consumed within the workshop (non-billable)</p>
      {/* TODO: internal use records list */}
      {/* TODO: log use form — select item from inventory, qty, purpose, date */}
    </div>
  )
}
