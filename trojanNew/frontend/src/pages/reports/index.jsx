export default function Reports() {
  return (
    <div>
      <h1 className="text-2xl font-semibold mb-1">Reports</h1>
      <p className="text-muted-foreground text-sm mb-6">Analytics and reporting for managers and admins</p>
      {/* TODO: tab nav for all 9 reports */}
      {/* 1. Stock report — current levels + below reorder threshold */}
      {/* 2. Stock movement report — in/out per item over date range */}
      {/* 3. Supplier purchase history — purchases per supplier, total spend */}
      {/* 4. Sales report — revenue by date, product, channel */}
      {/* 5. Service job summary — labor + parts revenue */}
      {/* 6. Internal use summary — by period */}
      {/* 7. Returns report — return rate, refund totals */}
      {/* 8. Profit margin report — company price vs selling price */}
      {/* 9. Low stock alerts */}
    </div>
  )
}
