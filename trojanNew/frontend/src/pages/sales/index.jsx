export default function Sales() {
  return (
    <div>
      <h1 className="text-2xl font-semibold mb-1">Direct Sales</h1>
      <p className="text-muted-foreground text-sm mb-6">Walk-in counter sales</p>
      {/* TODO: sales list */}
      {/* TODO: new sale form — optional customer, add items from inventory, payment method */}
      {/* TODO: confirm sale → stock decrements → generate PDF receipt */}
    </div>
  )
}
